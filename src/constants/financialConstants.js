export const DEFAULT_MARGIN_INTEREST = 0.08
export const DEFAULT_SAVINGS_INTEREST = 0.02
export const DEFAULT_TAX_RATE = 0.2

export const CURRENCY_FORMAT = new Intl.NumberFormat(undefined, {
  style: 'currency',
  currency: 'USD',
})
